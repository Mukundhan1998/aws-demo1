version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: latest
  build:
    commands:
      - echo "Building the application..."
      - mkdir artifacts
      - cp -r * artifacts/
  post_build:
    commands:
      - echo "Build completed successfully"
artifacts:
  files:
    - '**/*'
  base-directory: artifacts
